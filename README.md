# datetick
Tiny, immutable, fully timezone-aware date toolkit for JavaScript &amp; TypeScript — formatting, parsing, durations, calendar-grid data and live "time ago". Zero dependencies, framework-agnostic (Angular/React/Vue/Node).
